# ============================================================
# requirements-locked-amd-win.txt — Jet Tagging Project (renamed note: keep as requirements-locked.txt for CI)
# Pinned to the verified venv (Python 3.10.11, Windows, AMD GPU).
# For NVIDIA/Linux/Mac use requirements.txt + CUDA wheel (see arcefn/utils/device.py + REPRODUCING.md).
#
# IMPORTANT — AMD GPU / torch-directml constraints (verified 2026-08-15):
#   * This machine uses an AMD Radeon RX 5700 XT. The ONLY supported backend
#     is torch-directml. There is NO CUDA and NO CPU-only torch fallback.
#   * torch-directml==0.2.2.dev240614 is version-locked to torch 2.3.x.
#     Do NOT upgrade torch beyond 2.3.x (e.g. torch 2.12.0+cpu, which was
#     previously pinned here, does not exist and breaks torch-directml).
#   * `torch.__version__` reporting "2.3.1+cpu" is EXPECTED for a
#     torch-directml install — do not reinstall torch.
#   * numpy MUST stay <2 (1.26.4). numpy 2.x breaks torch-directml at import
#     time ("A module that was compiled using NumPy 1.x cannot be run in
#     NumPy 2.x") and breaks torch<->numpy interop.
#   * Do NOT pass --amp: mixed precision is CUDA-only (see train_arcface.py).
#   * Verify with:
#       python -c "import torch_directml; print(torch_directml.device_name(0))"
#     Expected output: AMD Radeon RX 5700 XT
# ============================================================

# --- Core ML (AMD GPU backend) ---
torch==2.3.1
torch-directml==0.2.2.dev240614
torchvision==0.18.1
numpy==1.26.4

# --- HEP physics ---
awkward==2.10.0
awkward-cpp==54
vector==1.8.1
h5py==3.16.0
hdf5plugin==7.0.0
fastjet==3.5.1.3
EnergyFlow==1.4.0

# --- Scientific computing ---
scipy==1.15.3
scikit-learn==1.7.2

# --- Visualization ---
matplotlib==3.10.9
seaborn==0.13.2
umap-learn==0.5.12

# --- Utilities & testing ---
tqdm==4.68.4
pytest==9.1.1
